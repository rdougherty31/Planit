//Create Trip button
import React from "react";
import "./style.css";

export default function LogoutBtn(props) {

    return (
        <button className="generalBtn" id="logoutBtn" {...props}>Logout</button>
    );
}